.DS_Store
__pycache__/
cache/source_hashes.pending.json

# 照合ツールの一時生成物（再生成可能）
cache/*.log
cache/apple_music_review_sheet.html
tools/__pycache__/

# 機械収集パイプラインの作業ファイル
# 正規化スナップショットは差分の詳細（追加行）を出すためのもので、
# 失っても cache/collect_state.json のハッシュで変化なし判定はできる
cache/normalized/
cache/ai_queue.json
cache/ai_result.json
cache/ai_accepted.json
cache/collect_metrics.jsonl
cache/collect_state.pending.json
cache/normalized_pending/

# ローカルLLMの実行結果・監査データ（再生成可能）
local_llm/

# ルートへ誤生成した場合の保険
ab-native-*.json
audit-*.zip
